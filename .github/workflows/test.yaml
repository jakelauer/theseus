name: Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/setup.yml

  build-package:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Run tests
        run: pnpm test
