import { expect } from "chai";
import { createLogger, format } from "winston";
import { theseusLogFormat, theseusTransports } from "../winston-config-builder";

describe("Winston Configuration Builder", () => {
    // Not testing this stuff
    expect(true).to.be.true;
});
